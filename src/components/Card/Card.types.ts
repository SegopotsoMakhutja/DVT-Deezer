export interface IArtistCardProps {
    name: string;
    nbFans: number;
    picture: string;
    artistId: string;
}

export interface IAlbumCardProps {
    name: string;
    title: string;
    albumArt: string;
    albumId: string;
}